# 审计工坊智能审计系统 —— 开发决策单

> 用途：Phase 0 验收门必需。请领导对 13 项开发决策逐条勾选（同意 / 不同意（填替代方案）/ 暂缓）。
> 硬阻塞项：**D4 / D5 / D8 / D13** 不确认，Phase 1、Phase 5 无法开工，请优先勾选。
> 填写后：每项勾选结果同步给开发，未确认项不得实施。

---

## 一、硬阻塞项（优先）

| # | 决策内容 | 建议 | 领导选择 |
|---|---|---|---|
| D4 | 延伸审计单位、报告文号、项目开始日期、进点日期、审计重点，是否纳入本轮「现有字段补持久化」？ | 纳入：做同名增量列，不改现有字段含义 | ✅ **已确认（2026-08-06）：除报告文号外全部纳入；报告文号留到文书阶段** |
| D5 | 旧项目阶段迁移：是否允许按现有数据（字段+事项）自动推断阶段，人工批量确认后标记 active？ | 允许：一次性推断 + 人工确认 | ✅ **已确认（2026-08-06）：允许自动推断 + 人工批量确认** |
| D8 | 采购数据、访谈数据：新增 `data_*` 表，还是映射 `data_general`？ | 新增两张骨架表 `data_procurements` / `data_interviews`（仅 project_id/document_trace_id/raw_text/extra_fields） | ✅ **已确认（2026-08-06）：新增两张骨架表，采购表先充实字段、访谈表先占位** |
| D13 | 「业务发生时间」是否必填？（方案A 已确认语义=被审计业务实际发生起止时间） | 非必填：部分项目无法精确界定 | ☐ 同意 ☐ 不同意（替代：___）☐ 暂缓 |

## 二、字段与年度口径

| # | 决策内容 | 建议 | 领导选择 |
|---|---|---|---|
| D1 | 「立项单位」是否等同当前创建人/所属组织？ | 等同 creator；若不同，用页面现有字段承载 | ☐ 同意 ☐ 不同意（替代：___）☐ 暂缓 |
| D2 | 审计年度取法？ | 取审计期间起始年份 | ☐ 同意 ☐ 不同意（替代：___）☐ 暂缓 |
| D3 | `f-target-unit` 与 `f-unit` 是否同一业务含义？ | 同一；审计对象允许填多单位 | ☐ 同意 ☐ 不同意（替代：___）☐ 暂缓 |
| D12 | 审计年度派生口径？ | 统一取 `audit_period` 第一个年份，无法解析兜底用创建年份并标记来源 | ☐ 同意 ☐ 不同意（替代：___）☐ 暂缓 |

## 三、智能分析相关（本轮只冻结契约，不实现）

| # | 决策内容 | 建议 | 领导选择 |
|---|---|---|---|
| D6 | 无精确条款或无文档页码的 AI 推荐，是否统一标记「待人工核实」并禁止进入最终文书？ | 是 | ☐ 同意 ☐ 不同意（替代：___）☐ 暂缓 |
| D7 | 音频转写本轮是否只规划目录和接口，后续对接既有转写服务？ | 是（本轮不新建音频算法） | ☐ 同意 ☐ 不同意（替代：___）☐ 暂缓 |
| D9 | 项目文档语义检索：补项目 chunks 向量索引，还是只走结构化+关键词？ | 本轮只走结构化+关键词，向量检索入 backlog | ☐ 同意 ☐ 不同意（替代：___）☐ 暂缓 |
| D10 | LLM 故障降级：允许纯规则降级（结果标记「非 AI 推理」）？ | 允许，降级结果强制标记，禁止混入最终文书 | ☐ 同意 ☐ 不同意（替代：___）☐ 暂缓 |
| D11 | 数值单位契约：全库金额类字段统一按「元」存储？ | 是（避免阈值比对差万倍） | ☐ 同意 ☐ 不同意（替代：___）☐ 暂缓 |

---

## 汇总

- 硬阻塞 D4/D5/D8：✅ 已确认（2026-08-06）；D13：✅ 方案A 已确认，必填性待定
- 全部 13 项：____ 已确认 / ____ 暂缓（D1/2/3/6/7/9/10/11/12 待定，可按建议默认推进）
- 领导签字：____________  日期：____________
