name: audit/记录留痕类/交接记录
type: model
description: 从记录留痕类类文档（交接记录）中提取结构化元数据。涵盖字段如：交接日期、处置方式、接收单位、车辆原所属机构。文档范围包括：国外机构退役车辆交接记录等。
guideline: '1. 从文档内容中提取所有可识别的元数据字段，逐字段返回原文精确值，不做归纳概括。

  2. 文档中未出现的字段返回「未提供」。

  3. 重点关注日期、金额、当事方名称/身份、审批/决策节点、合规性标志。

  4. 金额字段保留币种和单位（万元/亿元），日期格式统一为 YYYY-MM-DD。'
domain: audit
tags:
- 记录留痕类
- 审计
- 车辆
- 元数据提取
template_class:
  category: 记录留痕类
  member_types:
  - 交接记录
  member_count: 1
  member_details:
  - 国外机构退役车辆交接记录
output:
  fields:
  - name: 交接日期
    type: date
    description: 提取 交接日期 的日期值，格式 YYYY-MM-DD
  - name: 处置方式
    type: string
    description: 提取 处置方式 的文本值
  - name: 接收单位
    type: string
    description: 提取 接收单位 的文本值
  - name: 车辆原所属机构
    type: string
    description: 提取 车辆原所属机构 的文本值
  - name: 文档标题
    type: string
    description: 文档/文件的实际标题
    required: false
  - name: 文档日期
    type: date
    description: 文档创建/生成日期，格式 YYYY-MM-DD
    required: false
  - name: 文档编号
    type: string
    description: 文档唯一编号/文号（如有）
    required: false
  - name: 涉及单位
    type: string
    description: 文档涉及的主要单位/机构名称
    required: false
  - name: 涉及人员
    type: string
    description: 文档涉及的主要人员姓名
    required: false
  - name: 涉及金额
    type: number
    description: 文档涉及的总金额（如有，含币种）
    required: false
violations:
- expression: (车辆原所属机构、处置方式、接收单位、交接日期.处置方式 = '转交国内单位' AND 车辆原所属机构、处置方式、接收单位、交接日期.接收单位 NOT IN (规定的上交接收单位列表)) OR (车牌号、车辆来源（是否退役转籍）、当前使用部门、车辆型号、价格、排量、购置日期、使用部门、车辆类型（越野车标识）、车辆来源（国产/进口）、采购方式、名义权属单位、实际使用部门、车辆购置日期、车辆状态.车辆来源（是否退役转籍）
    = '退役车辆' AND 入账日期、来源说明、车辆编号、使用状态、权属单位.来源说明 LIKE '%捐赠%' AND NOT EXISTS (车辆原所属机构、处置方式、接收单位、
  description: 退役车辆未按规定上交而违规转给关联单位，或通过虚假捐赠、改装等方式变相继续使用进口车，均构成变相违规。
  audit_item: 违规配备公务用车
  suspicion: 变相进口或未上交退役车辆
  regulation: "[\n  {\n    \"法规名称\": \"党政机关公务用车管理办法\",\n    \"法规文号\": \"中办发〔2017〕71号\",\n    \"效力级别\": \"部门规章\",\n    \"条款号\"\
    : \"第十二条\",\n    \"内容概述\": \"党政机关应当按规定对公务用车进行报废、更新和处置；国外机构退役车辆应按规定上交或处置，不得擅自留用或转移给关联单位。\"\n  }\n]"
